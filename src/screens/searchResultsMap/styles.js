import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%'
    },

    map: {
        width: '100%',
        height: '100%'
    },

    items: {
        position: 'absolute',
        bottom: 6,
        marginLeft: 5
    }
})

export default styles;